extends baseEmail

block content
  h3 Hola #{work.User.name}, tiene un nuevo trabajo
  p Equipo : #{work.marca} | #{work.modelo}
  p Codigo : #{work.codigo}
  p Estado : #{work.State.name}
  p Total : $#{work.total}
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`https://www.novatechnologyargentina.com/myworks/${work.id}/${work.uuid}`, target='_blank') Ver trabajo
  p= url                
  p Si necesitas cualquier ayuda haznos saber sin problema estaremos aqui para ayudarte con mucho amor !
  p - Nicolas Trabichet , CEO